/**
 * Competitor analysis types — re-exported from the canonical audit.ts registry
 * for convenience within the competitor module.
 */

export type {
  CompetitorSite,
  CompetitorGap,
  CompetitorAnalysisResult,
} from '../types/audit'
