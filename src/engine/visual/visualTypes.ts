/**
 * Visual UX analysis types.
 * Re-exported from audit.ts (the canonical type registry) for convenience.
 * Import from here inside the visual module; import from audit.ts everywhere else.
 */

export type {
  VisualCheckResult,
  VisualPageChecks,
  VisualPageAnalysis,
  VisualAnalysisResult,
} from '../types/audit'
